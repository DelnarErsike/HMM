# build strategy that encourages the AI to generally do things that are beneficial for when it or its territory gets annexed
HMM_ai_annex_build_infra_until_off_toaster_eco = {
    enable = {
        OR = { # Don't apply this to "main" countries to prevent test build interference
            is_playable_country = no
            is_annexable_playable_country = yes
        }
		OR = {
            has_idea = undisturbed_isolation
			has_idea = isolation
            has_idea = civilian_economy
        }
	}
    abort = {
        NOR = {
            has_idea = undisturbed_isolation
			has_idea = isolation
            has_idea = civilian_economy
        }
    }
    ai_strategy = {
        type = building_target
        id = infrastructure
        value = 1000
    }
}

HMM_ai_annex_build_no_mils_until_gun2 = {
    enable = {
        OR = { # Don't apply this to "main" countries to prevent test build interference
            is_playable_country = no
            is_annexable_playable_country = yes
        }
		OR = {
            date < 1938.2.1
            has_idea = undisturbed_isolation
			has_idea = isolation
            has_idea = civilian_economy
        }
	}
    abort = {
        date = 1938.2.1
        NOR = {
            has_idea = undisturbed_isolation
			has_idea = isolation
            has_idea = civilian_economy
        }
    }
    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = -1000
    }
}

HMM_ai_annex_build_no_messing_up_deterrence = {
    enable = {
        OR = { # Don't apply this to "main" countries to prevent test build interference
            is_playable_country = no
            is_annexable_playable_country = yes
        }
	}
    ai_strategy = {
        type = building_target
        id = bunker
        value = -9999
    }
    ai_strategy = {
        type = building_target
        id = coastal_bunker
        value = -9999
    }
}
